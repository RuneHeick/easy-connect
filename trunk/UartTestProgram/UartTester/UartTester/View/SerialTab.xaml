<UserControl x:Class="UartTester.View.SerialTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition  Height="auto"/>
        </Grid.RowDefinitions>
        <DockPanel >
            <Button Margin="5" Command="{Binding StartCommand}">Start</Button>
            <ComboBox ItemsSource="{Binding Serial.Ports}" Margin="5" SelectedIndex="0" SelectedValue="{Binding SelectedPort, Mode=OneWayToSource}"> </ComboBox>
        </DockPanel>
    </Grid>
</UserControl>
